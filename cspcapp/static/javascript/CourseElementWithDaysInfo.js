class CourseElementWithDaysInfo {
    constructor(dict) {
        this.course_element_id = dict.course_element_id;
        this.course_id = dict.course_id;
        this.teacher_initials = dict.teacher_initials;
        this.days_as_string = dict.days_as_string;
    }
}